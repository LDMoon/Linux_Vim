# Compile all .cpp and run immediately 
alias carp="g++ *.cpp && ./a.out"

# Compile all .cpp AND .o files and run immediately
alias carpo="g++ *.cpp *.o && ./a.out"

# Opens all .cpp and .h files in tabs.
alias vimall="vim *.cpp *.h -p"
